import {DatePicker, Button} from 'antd'
import 'antd/dist/antd.css';

var Header = () => {
    return(
        <div>
            <Button>Login</Button>
            <DatePicker />
        </div>
    )
}

export default Header